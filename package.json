{
  "dependencies": {
    "axios": "^1.4.0",
    "json-server": "^0.17.3",
    "parcel-bundler": "^1.12.5"
  },
  "scripts": {
    "PARCEL": "npx parcel index.html",
    "JSONserver": "json-server -p 3005 --watch db.json --host 127.0.0.1"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  }
}
